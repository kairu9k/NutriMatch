# NutriMatch Capstone — To-Do List

Stack: Nuxt 4 + Tailwind CSS + Lucide icons (frontend, real team codebase) + Django 5 / DRF (backend, replacing Laravel) + PostgreSQL

**Frontend status (2026-09-04):** `frontend/` is now a clone of the team repo (`KPChiu7/NutriMatch-frontend`). Confirmed installed and running clean (`npm install` + `npm run dev` verified, no errors). This replaces the earlier from-scratch/Bootstrap plan — build on this codebase, don't rebuild it.

**Correction (2026-09-06):** local repo is actually checked out on `feature/landing-julia`, not `master` — the merge from PR #1 was done locally but never pushed to `origin/master` (which still only has the original bare Nuxt scaffold commit). `feature/landing-julia` has all the real work and is what CLAUDE.md's file/component descriptions refer to; kept as the working branch since user is now driving both frontend and backend directly (teammate branch-safety caveats no longer apply the same way — confirm before any push/force-op regardless).

**Scope decision (2026-09-04): Philippine food data only, for now.** FNRI Food Exchange Lists is the sole food source to build/seed/query. USDA FoodData Central integration is explicitly deferred — the `usda` schema option stays in `MealPlanFoodItem.source_type` for future extensibility, but no USDA service/endpoint gets built unless the user asks to revisit this later.

## Phase 0 — Backend setup ✅ DONE (2026-09-04)
- [x] Laravel deleted entirely, Django scaffolded directly in `backend/` (schema preserved in `database.txt`, no PHP reference kept)
- [x] `django-admin startproject nutrimatch_api .` + `.venv` (Python 3.13)
- [x] Installed: `django` (6.1.1), `djangorestframework`, `djangorestframework-simplejwt`, `django-cors-headers`, `python-decouple`, `django-filter`, `httpx`
- [x] SQLite for dev (Postgres not installed on this machine yet — switch before deployment, Phase 8)
- [x] `.env` / `.env.example` wired via `python-decouple` (SECRET_KEY generated, PayMongo/Daily.co/USDA keys stubbed, empty)
- [x] `django-cors-headers` configured for `localhost:3000`
- [x] Custom `User` model in `accounts` app (role enum, soft-delete `deleted_at`, email as USERNAME_FIELD), registered in Django admin, migrated, verified via `manage.py check` + dev server boot (302 redirect on `/admin/` confirms working)

## Phase 1 — Data layer ✅ DONE (2026-09-04)
Reference: `database.txt` (authoritative DBML schema, 21 tables/6 groups)
6 apps created matching the schema's own functional groups, each with models + admin registration:
- [x] `accounts` — `User` (done in Phase 0)
- [x] `profiles` — `RndProfile`, `RndLanguage`, `RndAvailabilitySchedule`, `ClientProfile`, `ClientHealthProfile`
- [x] `scheduling` — `RndClientRelationship` (unique constraint on rnd+client pair), `Appointment`, `ConsultationSession`, `Review` (1-5 rating check constraint)
- [x] `clinical` — `PreConsultationScreening`, `NcpRecord` (ONE record with all 4 phase sections as fields — assessment/diagnosis-PES/intervention/monitoring — matching `frontend/app/components/NCPRecords.vue`'s implementation), `ProgressRecord`
- [x] `nutrition` — `FoodExchangeCategory`, `FoodExchangeItem`, `MealPlan`, `MealPlanMeal`, `MealPlanFoodItem` (RA 10173 data-minimization rule documented directly in the model docstring — only `food_name` persisted from external lookups, never a nutrient payload)
- [x] `communication` — `Message`, `Resource`, `Reminder`, `NotificationLog`
- [x] `billing` — `Invoice` (commission_amt auto-freezes at creation via `save()` override, never recomputed), `PaymentTransaction`
- [x] `core` — `SystemSetting`, `ApiCache` (documented as the one deliberate exception to data minimization — TTL'd cache only), `AuditLog` (admin-locked read-only/no-delete to enforce immutability)
- [x] `sessions`/`migrations` tables intentionally NOT ported — Django's own session/migration machinery replaces them
- [x] All migrations generated + applied cleanly (`manage.py check` clean, 21/21 domain tables confirmed present via `apps.get_models()`)
- [x] Fixed a real bug found along the way: `UserManager.create_superuser`/`create_user` passed `role` both positionally and via `**extra_fields` (since `role` is in `REQUIRED_FIELDS`), causing `createsuperuser` to crash — fixed by always merging role into `extra_fields` before calling `_create_user`
- [x] Verified superuser creation + Django admin login end-to-end

## Phase 2 — Auth ✅ DONE (2026-09-04)
- [x] Custom `User` model with role field — done in Phase 0/1
- [x] SimpleJWT setup: `POST /api/auth/login/` (returns access+refresh+user, role/full_name embedded as JWT claims), `POST /api/auth/refresh/`, `GET /api/auth/me/`
- [x] `POST /api/auth/register/client/` — creates `User` + `ClientProfile` + `ClientHealthProfile` transactionally. Accepts `primary_health_concern` (maps to `health_goals` JSON list) to match `RegisterFlow.vue`'s form fields. Frontend calls the client role "patient" — normalized to `client` server-side, not a field name to copy literally.
- [x] `POST /api/auth/register/rnd/` — creates `User` + `RndProfile` (unverified by default — matches proposal's manual PRC verification requirement)
- [x] Role-based DRF permission classes: `IsAdmin`, `IsRnd`, `IsClient` in `accounts/permissions.py` — ready to use on Phase 3 endpoints, not yet applied anywhere
- [x] Verified end-to-end via curl: register client, register RND, duplicate-email rejection, login (correct tokens + claims), `/me` with and without token (401 when missing), wrong-password rejection, related profile rows actually created with correct data. Test users cleaned from dev DB afterward.
- [ ] Still TODO: wire `frontend/app/components/LoginFlow.vue` and `RegisterFlow.vue`'s `console.log`/`navigateTo` stubs to actually call these endpoints (part of Phase 6's "wire to real data" step) — not done yet, backend is ready and waiting

## Phase 3 — Core API endpoints ✅ DONE (2026-09-04)
All endpoints built, mounted under `/api/`, verified end-to-end via a full scripted flow (register → verify RND → search → request relationship → RND accepts → book appointment → confirm → screening → NCP create/finalize → messages → food exchange). Test data cleaned from dev DB after each run.

- [x] Admin (`accounts`): `GET /admin/users/` (list, filterable by role), `GET /admin/users/<id>/`, `DELETE /admin/users/<id>/` (soft delete), `PATCH /admin/users/<id>/toggle-active/`, `PATCH /admin/users/<id>/verify-rnd/`
- [x] Client matching (`profiles`): `GET /client/rnds/` (filter by specialty/language, **only verified + accepting-new-clients RNDs shown** — confirmed unverified RNDs correctly hidden), `GET /client/rnds/<id>/` (includes average rating), `GET/PATCH /rnd/profile/` (RND self-service)
- [x] Relationship lifecycle (`scheduling`) — found and filled a real gap: the schema/proposal only described client-requests-relationship, with no explicit accept step, which would've made every relationship stuck at `pending` forever. Added: `POST /client/rnds/<id>/request/`, `GET /rnd/relationship-requests/` (RND's inbox), `PATCH /rnd/relationships/<id>/accept/` (pending → active)
- [x] Client appointments: `GET/POST /client/appointments/` (booking blocked unless relationship is active — verified), `GET /client/appointments/<id>/`, `PATCH /client/appointments/<id>/cancel/`
- [x] RND appointments: `GET /rnd/appointments/`, `GET /rnd/appointments/<id>/`, `PATCH .../confirm/`, `PATCH .../complete/`, `PATCH .../cancel/` (each enforces valid status transitions)
- [x] Reviews: `POST /client/reviews/` (only for own completed, not-yet-reviewed appointments)
- [x] Pre-consultation screening (`clinical`): `POST /client/screening/` — runs the calculation engine live (see below), `GET /client/screening/<appointment_id>/`
- [x] NCP records (`clinical`): `GET/POST /rnd/relationships/<id>/ncp/`, `GET/PATCH /rnd/ncp/<id>/`, `PATCH /rnd/ncp/<id>/finalize/` (draft → completed)
- [x] Food exchange (`nutrition`, FNRI only): `GET /food-exchange/categories/`, `GET /food-exchange/items/` (filterable by category/search/diabetes-hypertension-renal flags)
- [x] Messages (`communication`): `GET/POST /relationships/<id>/messages/` (scoped to the caller's own relationship), `DELETE /relationships/<id>/messages/<id>/` (soft delete, sender-only)
- [x] **`clinical/services.py`** — the nutritional calculation engine (Mifflin-St Jeor BMR, TDEE, WHO Asia-Pacific BMI classification, NRS-2002 scoring skeleton). Verified against hand calculation: 70kg/170cm/age 31/male/moderately_active → BMI 24.22 ("Overweight At Risk" per WHO Asia-Pacific 23–24.9 bracket, correctly NOT "Normal"), BMR 1612.50, TDEE 2499.38 — all exact matches. Age is derived from `ClientProfile.date_of_birth` at request time, not stored redundantly.

## Phase 4 — External service integrations ✅ DONE (2026-09-04)
No real API keys yet (PayMongo/Daily.co `.env` values still blank) — built structurally correct against each provider's documented API contract, verified with 13 passing tests using mocked `httpx.Client` (no real network calls). Real keys can be dropped into `.env` later with zero code changes.

- [x] `billing/services.py` — `PayMongoService` (`httpx`-based): `create_payment_link` (PHP→centavos conversion verified), `handle_webhook` (HMAC-SHA256 signature validation via `hmac.compare_digest`, ported exactly from the original Laravel logic), `get_payment_status`
- [x] `POST /api/client/invoices/<id>/pay/` — client-initiated payment, only for own unpaid invoices
- [x] `POST /api/webhooks/paymongo/` — no JWT auth (`AllowAny` + `authentication_classes = []`, `csrf_exempt`), signature validated inside the service; on `payment.paid` marks invoice paid + writes `PaymentTransaction` + `AuditLog` row; on unmatched reference returns 200 anyway (don't make PayMongo retry an event that isn't ours to match)
- [x] `scheduling/services.py` — `DailyCoVideoService`: `create_room` (auto-expiry from `appointment.duration_minutes`), `_generate_host_token` (owner-privilege token for the RND), `delete_room`
- [x] Wired into `RndAppointmentConfirmView`: confirming a `video`-type appointment auto-provisions a `ConsultationSession` + attaches the safe `participant_url` to the appointment. Confirmation still succeeds even if Daily.co fails (gateway hiccup shouldn't block confirming an appointment) — RND can retry video setup separately.
- [x] **Security invariant preserved and test-verified**: `host_url` (RND-only, grants owner controls) is never serialized into any client-facing response — `ConsultationSessionSerializer` only exposes `video_provider`/`session_status`/timing fields, confirmed via an explicit `assertNotIn` test.
- [x] ~~`UsdaFoodService`~~ — still deferred, out of scope (PH food data only, see scope note at top)
- [x] `NutritionCalculatorService` → done early, in Phase 3 as `clinical/services.py`
- [ ] `AuditService` — not built as a separate abstraction; `AuditLog.objects.create(...)` is called directly where it matters so far (e.g. on invoice-paid). Revisit if repetition becomes a problem.
- [x] Found and fixed a real bug along the way: `Invoice.commission_pct` (Phase 1) and `MealPlanFoodItem.exchanges` (Phase 1) used bare float literals (`default=10.00`, `default=1.0`) as Django `DecimalField` defaults — before the first `.save()` round-trips through the DB, the in-memory value is still a Python `float`, and `Decimal * float` raises `TypeError`. This only surfaced now because Phase 1's tests never actually saved an `Invoice`. Fixed both to `Decimal("10.00")`/`Decimal("1.0")`. No migration needed (Django's migration serializer already normalized the stored default identically either way — this was a pure Python-runtime bug, not a schema bug). Worth grep'ing for this pattern (`DecimalField.*default=[0-9]*\.[0-9]`) if more float-literal defaults show up later.

## Phase 5 — Background jobs (if needed)
- [ ] Redis + Celery setup
- [ ] Port any queued jobs (notifications, webhook post-processing)
- [ ] `celery worker` running alongside `runserver` in dev

## Phase 6 — Frontend: fill the gap between `drive/` mockups and current codebase

**Already built (confirmed present, verify against drive/ mockup for fidelity, don't rebuild):**
landing (`index.vue`), login, register (client+rnd combined flow), admin-dashboard, rnd-dashboard, appointments, audit-logs, availability, billing-commission (admin's commission view), meal-planning, messages, ncp-records (all 4 phases in one component), resource-library, reviews, rnd-verification, system-settings, profile-settings, my-patients, client-management (admin's client oversight), earnings, platform-reports

**Missing — no page/component yet, need to build:**
- [x] **`about.vue`, `contact.vue`, `privacy-policy.vue`, `terms.vue` built (2026-09-06)** — static marketing pages, all real content (team names/adviser from the mockups, real RA 10173/PayMongo/PRC-verification language in the legal pages, no placeholder lorem ipsum). New shared components: `MarketingNavbar.vue` (simple top nav for standalone pages — `LandingNavbar.vue` couldn't be reused as-is since it's hardcoded to the landing page's own `#features`/`#how-it-works` in-page anchor IDs, which don't exist on these routes) and `SiteFooter.vue` (extracted from `LandingCTA.vue`'s inline footer markup so it's not duplicated across every new page; `LandingCTA.vue` now just renders `<SiteFooter />`, no visual change). Footer's previously-placeholder `href="#"` links for Privacy Policy/Terms/Contact now point to the real new pages. **`contact.vue` deliberately uses a `mailto:` link, not a fake-success form** — the mockup's form has a client-side-only "message sent!" toggle with zero backend (no `POST /contact/`, no email-sending service configured) — didn't want to build something that looks like it submitted but silently does nothing.
- [x] **`404.vue`/`500.vue` built (2026-09-06)** as a single `app/error.vue` per Nuxt convention (not `pages/`) — branches on `error.statusCode` for copy/title, shared visual shell. "Back to Home" calls `clearError({ redirect: '/' })` (verified this actually clears Nuxt's error state, not just a link to `/`). Verified live: visiting a nonexistent route renders the 404 correctly, "Back to Home" successfully returns to a working landing page.
- [x] **`forgot-password.vue`/`reset-password.vue` built for real (2026-09-07)** — OTP flow, not the mockups' fake emailed-link ("(Demo)" button that skipped the real flow entirely). New `accounts.PasswordResetCode` model (6-digit code, 15-min TTL, single-use) + migration, `accounts/services.py` (`request_password_reset`/`consume_reset_code`, reused `send_mail`), `POST /auth/password-reset/request/` (always returns the same generic success message whether or not the email exists, to avoid account enumeration) and `POST /auth/password-reset/confirm/`. `MAILERS['default']` now switches to real Gmail SMTP (`smtp.gmail.com:587`) once `GMAIL_HOST_USER`/`GMAIL_HOST_PASSWORD` (a Gmail **App Password**, not the account password) are set in `.env` — user is filling these in alongside PayMongo/Daily.co; blank still falls back to the console backend for local dev, matching the existing PayMongo/Daily.co "degrade to a clear no-op" pattern. Single new page `ForgotPassword.vue` (two-step: request email → enter code + new password) — `reset-password.vue` renders the same component rather than being a separate page, since the OTP flow has no emailed link to deep-link into. Verified live end-to-end: requested a code, pulled the real code from the dev DB, reset the password, logged in successfully with the new password; also verified a wrong code and a reused code are both correctly rejected. Test codes cleaned up after.
- [x] **`find-rnd.vue` built (2026-09-06)** — `FindRnd.vue` component + page, real data via `GET /client/rnds/`. Specialty (text, `icontains`) and language (dropdown) filters wired to the real query params the backend supports; consultation-mode filter and star ratings from the static mockup were dropped since neither exists on this endpoint (`RndSearchView` uses `RndProfileSerializer`, which has no `average_rating` — that's only added in `RndDetailView`) — didn't fake data that isn't there. Each card has a "Request" button calling `POST /client/rnds/<id>/request/` directly rather than linking to `rnd-profile-view.vue` (not built yet, separate TODO item) — `rnd-profile-view.vue` can link back here later once it exists. Added to client nav. Verified live: only verified+accepting RNDs shown (confirmed an unverified RND fixture correctly excluded), specialty filter narrows results correctly, Request creates a real `pending` `RndClientRelationship` row (confirmed via DB query), button becomes disabled/"Requested" after success.
- [x] **`book-appointment.vue` built (2026-09-06)** — `BookAppointment.vue` component + page. Added a new backend endpoint (`GET /client/relationships/`, `ClientActiveRelationshipsView` in `scheduling/`) since none existed to list which RNDs a client can actually book with — booking requires an ACTIVE relationship (`AppointmentCreateSerializer.validate_rnd_id` enforces this), and there was no way for the frontend to know which RNDs qualify. RND dropdown pre-selects from `?rnd=<id>` query param if present (for a future `rnd-profile-view.vue` "Book" link) or defaults to the first active relationship. Consultation type (video/chat/in-person), native `datetime-local` picker, duration (30/60/90 min), optional notes — submits to `POST /client/appointments/`. Deliberately did NOT replicate the static mockup's fake "available/unavailable" time-slot grid — no backend endpoint computes real slot availability (`RndAvailabilitySchedule` exists but isn't exposed via API), so a real datetime picker was used instead of faking availability data. Empty state (no active relationship yet) links to `find-rnd.vue`. Entry points added: "Book Appointment" button in `Appointments.vue` header (client only) and its empty state. Verified live end-to-end: booking against a real active relationship correctly creates a `pending` `Appointment` row, redirects to `/appointments`, and the new appointment renders correctly with working Cancel action. Test appointment cleaned up after.
- [x] **`client-dashboard.vue` built (2026-09-06), Overview tab only** — the static mockup has 8 tabs (Overview, Health Screening, My Tasks, Log Progress, Consultation Summaries, Resources, Reminders, Billing); deliberately scoped to Overview only since the other 7 need backend features that don't exist yet (task assignment, food/vitals logging, reminders CRUD, resource upload) — building them would mean faking data in the frontend, which this project has consistently avoided. Overview shows only real data: welcome banner (greeting + upcoming appointment or active-RND summary), stat cards (BMI/TDEE/NRS-2002/next appointment — each shows "—" when no data exists rather than a fake number), "Your RND" card (from `GET /client/relationships/`), "Latest Screening" card (from new `GET /client/screening/latest/` endpoint, added since none existed to fetch a client's own most recent screening without already knowing an appointment ID). Set as the client's post-login/post-register landing page (`LoginFlow.vue`/`RegisterFlow.vue` redirects updated from the `/appointments` placeholder), added to top of client nav. Health Screening + Consultation Summaries tabs are good near-term follow-ups (real data already exists, just needs small new read endpoints); Tasks/Logging/Reminders/Billing-on-dashboard need actual new backend features first — logged as separate future work, not attempted here. Verified live: real BMI 24.22/BMR 1613/TDEE 2499 rendered correctly with correct "Overweight (At Risk)" badge, active RND card, empty states correct when no screening/appointment exists. Test screening cleaned up after; client profile DOB/sex fixture kept (needed for BMR calc, reusable for future testing).
- [x] **`client-detail.vue` built (2026-09-06), one new endpoint, phase progress derived not hardcoded** — dynamic route `pages/client-detail/[relationshipId].vue`. Backend had almost everything already (NCP records, progress records, meal plans, appointments all list/filterable by relationship from prior work this session) except a way for an RND to view a specific client's `ClientProfile`/`ClientHealthProfile` — that data was `IsClient`-only before. Added `GET /rnd/relationships/<id>/client-profile/` (`RndClientProfileView` in `profiles/`), scoped to the RND's own relationship — never an open client lookup — reusing the existing `ClientProfileSerializer`. No new endpoint needed for appointments/NCP/progress: the page fetches the RND's full lists (`/rnd/appointments/`, `/rnd/relationships/<id>/ncp/`, `/rnd/relationships/<id>/progress/`) and filters client-side by this relationship, since those were already relationship-scoped or easily filterable. **NCP "phase progress" (mockup: Assessment ✓, Diagnosis ✓, Intervention 3, Monitoring 4) is derived honestly from which fields are actually filled** (`pes_problem` present → Diagnosis done, `diet_prescription` present → Intervention done, etc.) rather than a fake/hardcoded per-phase completion flag, since the model has none. Discovered along the way: `MyPatients.vue` (the natural link-in point) is itself still fully mock-driven (`~/mock/mockDatabase.js`) with fake patient IDs that don't map to real relationships — didn't link from it since that would require touching its mock data or partially wiring it, both extra scope; page works standalone via direct URL, logged the `MyPatients.vue` wiring gap below. Verified live end-to-end: real client "Test Client, 31 y/o Male, Type 2 Diabetes" banner, real Health Profile block (conditions/allergies/restrictions/goals), correct empty states (no NCP/vitals/appointment yet), then created a real NCP record and confirmed phase progress + vitals + full PES statement all rendered correctly. Full test suite (13 tests) still passes. Test data cleaned up after.
- [x] **`MyPatients.vue` wired to real data (2026-09-06) — with a real bulk (not N+1) Condition column** — the table needed Condition (from `ClientHealthProfile`, per-client) alongside Status/Last Visit/Next Appointment/NCP Status (all relationship/appointment/NCP-record-derived), and the user explicitly wanted real data shown, not a dropped column or a per-row N+1 fetch. Built `GET /rnd/patients/` (`RndPatientListView` + new `RndPatientListSerializer` in `scheduling/`) as one query for relationships plus two `Prefetch(..., to_attr=...)` calls (appointments, NCP records) — verified via `CaptureQueriesContext` at exactly 3 queries total regardless of patient count, not N+1. `condition` reads `client.health_profile.medical_conditions[0]` via `select_related`, no extra query. `last_visit`/`next_appointment` are computed from the prefetched appointments (most recent completed / soonest upcoming pending-or-confirmed), `ncp_status` from the most recent prefetched NCP record's `status` (defaults to `"not_started"` with zero records) — all real, none hardcoded. Also added `PATCH /rnd/relationships/<id>/decline/` (`RndRelationshipDeclineView`) since no decline/reject action existed at all before — sets status to `discharged` (the closest fit; the model has no distinct "declined" state) with `ended_at` set. Table's "View Chart" link now correctly points to `/client-detail/<relationship-id>/`, closing the loop flagged as a gap when `client-detail.vue` was built earlier this session. Verified live end-to-end: real patient row with real condition "Type 2 Diabetes," correct filter tab counts, "View Chart" correctly navigating to the matching real `client-detail.vue` page; decline endpoint tested via a real second pending relationship, confirmed transitions to `discharged`. Full test suite (13 tests) still passes.
- [x] **`consultation-room.vue` built (2026-09-06), plain iframe embed, no custom call UI** — unlike invoices/progress-records, no backend gap here: `Appointment.video_session_url` (the client-safe `participant_url`) was already exposed via `AppointmentSerializer`, and `RndAppointmentConfirmView` already auto-provisions the Daily.co room on confirm (Phase 4). The mockup's custom mute/camera/screen-share buttons and fake chat panel over a stock photo would all be non-functional without building custom WebRTC UI on Daily.co's JS SDK — a much bigger task. Instead: a plain `<iframe :src="appointment.video_session_url">` — Daily.co rooms are natively embeddable, so real mute/camera/screen-share/chat controls come from Daily.co's own UI inside the iframe, not fake buttons. New dynamic route `pages/consultation-room/[id].vue` with `layout: false` (fullscreen call view, no dashboard sidebar). Side panel shows the appointment's real pre-consultation screening (BMI/NRS/TDEE via the existing per-appointment `GET /client/screening/<appointment_id>/`) with a correct empty state when none is linked — not the mockup's fabricated numbers. RND sees an "Open NCP Record" link the client doesn't. "Join Call" button added to `Appointments.vue` for both roles, shown only when `status === 'confirmed' && type === 'video' && video_session_url` exists — never shown for a confirmed video appointment whose Daily.co room creation failed (which is the current dev-env default, no live Daily.co API key yet). Verified live end-to-end for both roles: correct fullscreen layout, correct other-party name in header (client sees RND, RND sees client), correct "no video room" and "no screening" empty states, RND-only NCP link present only for the RND view. Test appointment cleaned up after.
- [x] **`invoices-billing.vue` built (2026-09-06), plus a real invoice-generation gap fixed** — before this, nothing in the entire codebase ever created an `Invoice` row (`InitiatePaymentView`/`PayMongoWebhookView`/`PayMongoService` were all fully built and waiting from Phase 4, but with no upstream trigger). Fixed at the natural point: `RndAppointmentCompleteView` (`scheduling/views.py`) now auto-creates an `Invoice` (amount = the RND's current `consultation_fee`) the first time an appointment is marked completed — idempotent (checks `Invoice.objects.filter(appointment=...).exists()` first) in case complete is somehow called twice. Added `GET /client/invoices/` (`ClientInvoiceListView` in `billing/`) with a new `InvoiceListSerializer` that nests the appointment/RND for display — kept separate from the existing flat-FK `InvoiceSerializer` used by the pay flow, so `InitiatePaymentView`'s response shape didn't change. Frontend deliberately drops the mockup's payment-method radio-button modal (GCash/Card/Online Banking) — the real `PayMongoService.create_payment_link` doesn't take a method param at all, it returns one `payment_url` and PayMongo's own hosted page handles method selection; a fake local modal would only add a step that does nothing. Also dropped the mockup's "Download Receipt" button for paid invoices — no receipt-generation endpoint exists. Verified live end-to-end: booked → confirmed → completed a real appointment, invoice auto-generated for ₱500.00 (RND's real fee), list endpoint returned it correctly, "Pay Now" correctly surfaced `PayMongoService`'s real "PAYMONGO_SECRET_KEY is not configured" error (expected — no live keys yet) rather than crashing or faking success. All 13 existing billing tests still pass.
- [x] **`meal-plan-view.vue` built (2026-09-06), plus real meal-plan CRUD; existing mock builder left alone** — biggest gap yet: `MealPlan`/`MealPlanMeal`/`MealPlanFoodItem` had zero API surface (only `food-exchange/categories|items` existed in `nutrition/`), AND the existing 533-line RND-side `MealPlanning.vue` builder (already listed as "done" in Phase 6, wrongly — it reads/writes `~/mock/mockDatabase.js`, not real data) is entirely fake. Built the minimum real backend to give the client page genuine data without touching that builder: `POST/GET /rnd/relationships/<id>/meal-plans/`, `POST /rnd/meal-plans/<id>/meals/`, `POST /rnd/meals/<id>/food-items/` (each ownership-scoped to the RND's own relationship, same `validate_relationship` pattern as NCP/progress records), and `GET /client/meal-plans/`. New `MealPlanSerializer`/`MealPlanMealSerializer`/`MealPlanFoodItemSerializer` in `nutrition/serializers.py`, properly nested (plan → meals → food_items) so the client gets one call with everything. **Deliberately left `MealPlanning.vue` mock-driven** — wiring a 533-line existing builder to these new endpoints is separate, larger follow-up work, not something to bolt onto a read-only client page; logged below. Frontend shows real exchange totals (rice/meat/vegetable/fruit/milk/fat), per-meal exchange summaries computed from actual `MealPlanMeal` fields (not hardcoded like the mockup), and real food items with notes — correct empty states at both the "no plan yet" and "no meals in this plan yet" levels. Verified live end-to-end: RND created a real plan → added a breakfast meal → added 2 real food items → client's page rendered all of it correctly (1800 kcal, 5.0/3.0/4.0/2.0/1.0/3.0 exchange totals, "1 Rice · 1 Meat · 1 Veg" summary, both food items with notes). Full test suite (13 tests) still passes. Test data cleaned up after.
- [x] **`MealPlanning.vue` rebuilt for real data (2026-09-06) — required a UI redesign, not a re-plumb** — the old mock UI modeled meal plans per-day-of-week with raw kcal/carb/protein/fat typed per food item; the real schema has no `day_of_week` concept at all (one plan, meals by `meal_time` only) and expresses food in FNRI exchange counts, not raw macros — the actual clinical methodology this capstone is built around. Wiring the old UI as-is would have meant silently dropping the day-tabs feature or fabricating fake exchange numbers from typed-in macros, so the UI was redesigned to match the real model instead: a patient picker (new `GET /rnd/relationships/active/` — the same "active relationships" gap `MyPatients.vue` below needs), one plan per patient with an editable name/condition/target-kcal create form, meals added by time-of-day (breakfast through bedtime snack, each addable once), and per-meal exchange-count inputs (rice/meat/veg/fruit/milk/fat) edited directly and saved on blur. Added the missing update/delete endpoints this needed: `PATCH /rnd/meal-plans/<id>/`, `RETRIEVE/PATCH/DELETE /rnd/meals/<id>/`, `DELETE /rnd/food-items/<id>/` (create-only endpoints existed before, no way to edit or remove anything). **Caught and fixed a real bug during testing**: the first version auto-added a food item's exchange count to the parent meal's matching category total on create, but had no way to reverse that on delete (`MealPlanFoodItem` has no category field when free-typed, only when linked to a real `FoodExchangeItem`) — deleting a food item left the meal's exchange total silently wrong. Fixed by making food items a plain list (what was actually eaten) fully independent from the meal's exchange-count inputs (the RND's clinical prescription) — no auto-sync, matching how `meal-plan-view.vue` already displays them. Verified live end-to-end: created a real plan, added a meal, edited its Rice exchange count from 1→2 (persisted across reload), added and removed a food item with the exchange total staying correct throughout. Full test suite (13 tests) still passes.
- [x] **`notifications.vue` built (2026-09-06) — the biggest of the API-surface gaps closed this session** — worse than invoices/progress-records: `NotificationLog` had zero API surface AND was schema-shaped for delivery tracking (email/SMS/push status), not an in-app read/unread feed — no `is_read` field existed at all. Required a migration (`communication/migrations/0002_notificationlog_is_read.py`, adds `is_read` boolean) plus a small `communication/services.py::notify()` helper (creates an `IN_APP`/`DELIVERED` `NotificationLog` row — in-app notifications have no external delivery step to track, unlike email/SMS) reused across 3 real trigger points, not fabricated: message send (`MessageListCreateView.create`), appointment confirmation (`RndAppointmentConfirmView`), and invoice creation (`RndAppointmentCompleteView`, alongside the existing auto-invoice logic). Added `GET /notifications/`, `PATCH /notifications/<id>/read/`, `PATCH /notifications/mark-all-read/`. Reused the existing `notifiable_type` field (already on the model, previously unused) to carry the notification kind (`"appointment"`/`"message"`/`"invoice"`) rather than adding a new field — drives the icon per row on the frontend. **Left the existing `NotificationDropdown.vue` (admin layout's header bell widget) untouched** — it's admin-only, mock-driven, and wiring it is separate scope (admin/system notifications are a different concern from a client's own appointment/message/invoice feed); the dashboard layout's own bell icon is dead code inside a commented-out topbar block, so nothing to wire there either — added a plain "Notifications" nav link under Account instead. Verified live end-to-end: triggered all 3 real notification types (sent a message, confirmed a video appointment, completed it to generate an invoice), all appeared correctly with correct icons/relative timestamps/unread styling, "Mark All as Read" correctly cleared unread state and hid itself. Full test suite (13 tests) still passes. Test data cleaned up after.
- [x] **`pre-consultation-screening.vue` built (2026-09-06), biometrics only, NRS-2002 checkboxes deliberately dropped** — mockup's 3 NRS-2002 checkboxes (weight loss, reduced intake, chronic illness) map to a real `calculate_nrs2002()` in `clinical/services.py`, but that function was already dead code before this: `ScreeningCreateView.create()` never called it, and the serializer has no input fields for those checkbox answers. Wiring it up properly (new serializer fields + endpoint changes) was scoped out as separate future work rather than bundled into a frontend page — so this form only covers height/weight/activity-level/symptoms, which was already fully working. Submits to `POST /client/screening/`, shows real computed BMI/BMI-category/BMR/TDEE from the response (not re-derived client-side). Falls back to a note pointing at Profile Settings if BMR/TDEE come back null (means client's `date_of_birth`/`sex` aren't set yet — the calculation engine requires them). Linked from `ClientDashboard.vue`'s "Latest Screening" empty state via a new "Complete Screening" button (not added to persistent nav — matches how `book-appointment.vue` is reached contextually, not as a nav item). Verified live end-to-end: real screening (170cm/70kg/sedentary) produced correct BMI 24.22/BMR 1613/TDEE 1935, dashboard immediately reflected the new values after redirect. Test screening cleaned up after.
- [x] **NRS-2002 checkboxes/scoring wired up for real (2026-09-06)** — `calculate_nrs2002()` was dead code before this. Added `reduced_intake`/`has_chronic_illness` as write-only inputs on `PreConsultationScreeningSerializer` (not stored on the model — same pattern as `activity_level` feeding TDEE without being a "result" field), and `ScreeningCreateView.create()` now actually calls `calculate_nrs2002()` and saves the real `nrs_score`/`nrs_risk`. **`weight_loss_pct` — the third input the function needs — is deliberately NOT a checkbox**: the mockup's "unintentional weight loss" toggle had no way to express a percentage, so instead it's computed server-side by comparing this screening's `weight_kg` to the client's own most recent prior screening (a first-ever screening has no prior data and contributes 0 automatically) — a real, derived clinical signal rather than a vague self-reported yes/no. Verified live with three real scenarios: first screening with no risk factors → score 0/no_risk; second screening with a genuine 14.3% weight drop + chronic illness checked → score 3/at_risk (nutritional_score correctly maxed out by the weight-loss branch per the existing `elif` priority in `calculate_nrs2002`, chronic illness contributing its point on top); third screening at unchanged weight with only "reduced intake" checked → score 1/no_risk. Frontend (`pre-consultation-screening.vue`) shows the 2 real checkboxes plus a note explaining weight-loss is calculated automatically, and a 4th result box for the NRS-2002 score/risk badge. Full test suite (13 tests) still passes. Test screenings cleaned up after.
- [x] **`progress-tracker.vue` built (2026-09-06), plus two new endpoints, no BMI chart** — like invoices, `ProgressRecord` had zero API surface at all (no create, no list). The mockup's own subtitle ("recorded by RND Ivy Hope Alba") and the DBML schema (`progress_records.relationship_id`, RND-authored clinical monitoring) both confirm this is RND-logged data, not client self-reported — so this needed two new endpoints, not one: `POST /rnd/relationships/<id>/progress/` (`RndProgressRecordListCreateView`, same relationship-ownership-check pattern as the existing NCP record endpoint) and `GET /client/progress/` (`ClientProgressRecordListView`, client's own history across all relationships). **Dropped the mockup's BMI trend chart** — `ProgressRecord` has no `bmi` or `height_cm` field at all (only weight/BP/glucose/HbA1c/adherence), so there's no real per-record BMI to chart; faking one by reusing a single screening's height would misrepresent trend data. Built two lightweight inline-SVG line charts (`TrendLine.vue`, new small reusable component — no chart library added as a dependency for just 2 small charts) for Weight and Glucose trends, since both have real time-series data. Stat cards show "—" for BP/adherence when absent rather than a fake number. Verified live end-to-end: RND logged two real progress records via the API, client's tracker page correctly showed latest stats (68.50 kg, 118/78, 98.00 mg/dL, 90%), correct weight delta (↓1.5 kg), both trend charts rendered correctly, and full record history table with real RND notes. Test records cleaned up after.
- [x] **`resource-upload.vue` built (2026-09-06), Link type only — the last Phase 6 missing page** — `Resource` had zero API surface, and `file_path` is a plain `CharField` with no Django `FileField`/`MEDIA_ROOT`/`MEDIA_URL` configured anywhere in this project (same pattern as `User.profile_photo` — also just a URL string). Real PDF/video upload needs actual media-storage infrastructure, not just an endpoint — out of scope here. Added `POST/GET /rnd/resources/` and `PATCH /rnd/resources/<id>/` (`RndResourceListCreateView`/`RndResourceUpdateView` in `communication/`) with a new `ResourceSerializer` that **validates `type == 'link'` at creation** and rejects `pdf`/`video`/`article` with a clear "isn't wired up yet" message — deliberately refusing rather than silently accepting a create that can't actually attach a file. Also added `GET /client/resources/` (`ClientResourceListView`) — `Resource` has no per-relationship scoping (`uploaded_by` only), so it's a general library; client sees active resources from any RND they have an *active* relationship with. Frontend's "Add Resource" form only has Title/Description/URL fields (no fake file-drop zone) with an explicit note explaining file upload isn't available. **Fixed a nav mismatch discovered along the way**: RND sidebar's "Resources" link pointed at `/resource-library` (mockup: client-facing browse page, mock-driven `Resources.vue`) instead of the RND's own management page — now correctly points to `/resource-upload`. Verified live end-to-end: created a real link resource, confirmed pdf/video creation is correctly rejected, deactivate/activate toggle works and persists. Full test suite (13 tests) still passes. Test data cleaned up after.
- [x] **`resource-library.vue` built for real (2026-09-06) — turned out to be a naming collision, not a wiring task** — investigating this found that `Resources.vue` (rendered by `resource-library.vue`) was actually RND-facing upload/manage UI ("Upload nutrition education materials for your patients," Edit/Mark Inactive/Delete menu) mock-driven from `~/mock/mockDatabase.js` — a duplicate of what `resource-upload.vue` now does for real, not the client-facing browse page the name and mockup (`resources-library.html`, a card grid with type filters) implied. **There was no real client-facing resource browse page in the codebase at all.** Built one for real: new `ResourceLibrary.vue` calling the existing `GET /client/resources/` (from the `resource-upload.vue` work), with search + type filter chips (All/PDF/Video/Article/Link) matching the mockup's card-grid layout, each card showing real title/description/type badge/relative-time/an "open" button for link resources (no fake download/play buttons for PDF/video — nothing plays or downloads since file upload isn't implemented, per the `resource-upload.vue` scope decision). Deleted the now-redundant mock `Resources.vue` and added a "Resources" nav entry to the client sidebar — it never had one before, unlike the RND sidebar. Verified live: real "DOH Pinggang Pinoy Guide" link resource rendered correctly in the grid, type filter correctly narrowed to "No resources match this filter" for PDF. Full test suite (13 tests, backend untouched) still passes.
- [x] **`rnd-profile-view.vue` built (2026-09-06), no fake availability grid** — dynamic route `pages/rnd-profile-view/[id].vue` (first dynamic route in the frontend). Backend already had everything except a public reviews list and review count: added `GET /client/rnds/<id>/reviews/` (`RndPublicReviewsView` in `profiles/`) with a new `PublicReviewSerializer` that exposes only first-name + last-initial for the reviewer, not the full `UserSerializer` (RA 10173 PII-minimization, same pattern as `meal_plan_food_items.food_name`-only); also added `review_count` alongside the existing `average_rating` on `RndDetailView`. **Deliberately dropped the mockup's weekly-availability grid** (Mon 9–5, Tue 9–5, etc.) — `RndAvailabilitySchedule` model is real but there's no read endpoint for clients OR a write/management endpoint for RNDs yet; building a fake-looking grid with no real data behind it, or half-building just the read side with nothing to populate it, were both worse than leaving it out — logged as a real future feature gap, not an oversight. Also dropped the mockup's fabricated "10+ years experience / 180+ patients helped / 98% response rate" stat row entirely (no backing data exists for any of them). Wired into `FindRnd.vue` (RND name/avatar now links here) and `BookAppointment.vue` (arrives via `?rnd=<id>` query param, pre-selecting that RND). Sidebar CTA is state-aware: "Request to Connect" (no relationship) → "Request Sent" (pending) → "Book Appointment" linking to `/book-appointment?rnd=<id>` (active) — computed from `GET /client/relationships/`, not guessed. Verified live end-to-end: find-rnd → profile → book-appointment full chain works, real PRC license/fee/bio/empty-reviews-state all render correctly, active-relationship state correctly shows "Book Appointment" instead of "Request to Connect".
- [x] ~~RND weekly availability~~ — done 2026-09-07, see entry above.
- [x] **`rnd-appointments.html` scope reconciled with `Appointments.vue` (2026-09-07)** — found 2 real gaps while comparing, both fixed: (1) **"View Chart" link was missing** for RND-side confirmed/completed appointments (mockup has it, real component only had "View NCP Record" for completed) — added, links to the real `client-detail/<relationshipId>` page. (2) **The mockup's "Awaiting Screening" confirm-block had no backend enforcement at all** — any pending appointment could be confirmed with zero screening on file. Added a real gate: `RndAppointmentConfirmView` now 403s with a clear message if the client has never submitted a `PreConsultationScreening`. Deliberately loosened from the mockup's literal intent (screening tied to *this specific appointment*) to "has ever screened, for any appointment" — `PreConsultationScreeningSerializer`/the screening form have no appointment-picker at all, so a true per-appointment gate would need frontend changes there too; this was called out and the looser-but-real version was the explicit choice. Also fixed a real pre-existing dead-end bug exposed by the new gate: once `actionError[appt.id]` was set, the Confirm/Decline buttons vanished permanently with no retry path short of a full page reload — buttons now always render alongside the error note. "Reschedule" (mockup) stays dropped, no backend endpoint. Verified live: screening-less client correctly blocked with the real error message, submitting a real screening unblocked confirm immediately, View Chart correctly navigates. Full test suite caught a real regression from the new gate (an existing billing test confirmed a video appointment with no screening on file) — fixed by adding a screening fixture to that test; 13/13 tests pass.

**Auth wiring ✅ DONE (2026-09-06):**
- [x] Pinia installed (`pinia` + `@pinia/nuxt`); `useAuthStore` (`app/stores/auth.ts`) holds `user`, `rndProfile`, `accessToken`/`refreshToken`, persisted to `localStorage`, hydrated on app start (`app.vue`)
- [x] API composable (`app/composables/useApi.ts`) — injects JWT bearer header via `runtimeConfig.public.apiBase` (from `.env`/`NUXT_PUBLIC_API_BASE`), auto-retries once via `/auth/refresh/` on 401
- [x] `LoginFlow.vue` wired to `POST /auth/login/` (regular + admin login), inline errors, loading state, role-based redirect (client→`/appointments` placeholder, rnd→`/rnd-dashboard`, admin→`/admin-dashboard`)
- [x] `RegisterFlow.vue` wired to `POST /auth/register/client/` and `.../rnd/` — step 2 now branches fields by role (patient: DOB/gender/health concern; RND: PRC license number/specialization, previously missing from the form). Auto-logs in on successful registration.
- [x] Verified live end-to-end via browser automation (not just typecheck): client register→login→redirect, wrong-password 401 handling, RND register→login→redirect — all confirmed via Django server logs + DB inspection. Test accounts cleaned up after each run.
- [x] `dashboard.vue` layout made role-aware: sidebar profile card pulls real user from `useAuthStore()` instead of hardcoded "RND Ivy Hope Alba"; nav branches by role — RND keeps full nav, client gets a **minimal nav (Messages, Reviews, Log Out only)**. Logout now actually clears the auth store.
- [x] RND profile data (`GET /rnd/profile/`) fetched into `auth.rndProfile` after RND login and on hydrate — sidebar now shows real `specialization`/`prc_license_number`/`is_verified` instead of placeholder text. Verified live with a real verified RND account.
- [x] **`Appointments.vue` rewritten (2026-09-06)** — no longer mock-driven. Fetches `GET /client/appointments/` or `/rnd/appointments/` depending on role; client sees the RND's name + Cancel action, RND sees the client's name + Confirm/Decline (pending) or Mark Completed/Cancel (confirmed) + View NCP Record (completed). Wired to real transition endpoints (`.../confirm/`, `.../complete/`, `.../cancel/`), list re-fetches after each action, inline per-row error handling. Added back to client nav. Verified live end-to-end for both roles with a real relationship + 2 appointments fixture (pending→confirm/decline, confirmed→complete/cancel, client cancel) — all transitions worked correctly against the real DB. Test data cleaned up after.
- [x] **`ProfileSettings.vue` rewritten (2026-09-06)** — added a new backend endpoint (`GET/PATCH /client/profile/`, `MyClientProfileView` in `profiles/`, mirroring the existing `MyRndProfileView` pattern) since none existed for clients before. `ClientProfileSerializer` now nests `health_profile` (`source="user.health_profile"`). Frontend: tabs branch by role — client gets Personal Info/Health Info/Security (RND keeps Personal Info/Professional Profile/Languages/Fees & Payouts/Security); "PRC Verified" pill only shows for RND. Client's Personal Info tab is real (address/emergency contact, editable + saved via PATCH); Health Info tab shows real `ClientHealthProfile` data (medical conditions/allergies/dietary restrictions/health goals) as read-only — editing that is RND/clinical-workflow territory (screening, NCP records), not a self-service settings page. **Scope note:** editing first_name/last_name/email/phone on `User` itself isn't possible anywhere in the backend yet (`UserSerializer` is all read-only, `MeView` is GET-only) — Personal Info shows these as read-only for both roles rather than building fake-looking dead controls; a real fix is a separate task if needed later. Added back to client nav. Verified live for both roles incl. a real save round-trip (address/emergency contact persisted to DB, confirmed via direct query) — caught and fixed a real bug in the same pass: RND's `loadProfile()` returned early without clearing `isLoading`, leaving the panel stuck on "Loading…" forever for RND accounts.
- [x] **`Messages.vue` wired to real data (2026-09-07)** — was still fully mock-driven (`~/mock/mockDatabase.js`) despite being listed as already built; backend (`GET/POST /relationships/<id>/messages/`) was already real and unused by the frontend. No new endpoints needed — conversation roster comes from the existing `GET /rnd/relationships/active/` (RND) / `GET /client/relationships/` (client), one thread per active relationship. Open thread polls every 5s for new messages (no WebSocket infra in this project — matches the polling-vs-realtime open question below). Dropped the mockup's fake "video call" button and "Active now" presence dot — no real data backs either. Verified live end-to-end with two real sessions (client ↔ RND): message sent by client appeared correctly for the RND, RND's reply was picked up by the client's poll within 5s, and the existing message → notification pipeline fired a real "New message" notification. Test messages cleaned up after.
- [x] **`Reviews.vue` (RND-facing) wired to real data (2026-09-07)** — added `GET /rnd/reviews/` (`RndReviewListView` in `scheduling/`), reusing the existing `ReviewSerializer` unchanged. Average rating and the 5-star breakdown are computed client-side from the real list (small dataset, same pattern as other pages that don't have a dedicated summary endpoint). Caught and fixed a real bug during testing: the new view referenced `Review` without importing it (`NameError` at request time) — `scheduling/views.py` had never needed a direct `Review` import before since `ReviewCreateView` only touched it through the serializer. Verified live with a real 5-star review: correct 5.0 average, breakdown bar, and review card. Test review cleaned up after.
- [x] **`Earnings.vue` wired to real data (2026-09-07)** — added `GET /rnd/invoices/` (`RndInvoiceListView` in `billing/`) with a new `RndInvoiceListSerializer` (nests `client_name` via the relationship, computes `net = amount - commission_amt` since net isn't stored). Gross/commission/net/pending summary, the 6-month trend chart, and the invoice table are all derived client-side from this one list; the period dropdown (This Month/Last Month/Last 3 Months/This Year) now really filters by `created_at` instead of being inert. Dropped the mockup's hardcoded "15%" commission label (actual `commission_pct` is per-invoice, defaults to 10%) and the fake "↑12% vs last month" delta. Verified live with two real invoices (₱500 paid, ₱750 unpaid): correct ₱1,250 gross/₱125 commission/₱1,125 net/₱750 pending, correct trend bar in the current month, correct Paid/Pending pills. Test invoices cleaned up after.
- [x] **RND weekly availability built (2026-09-07)** — closes the gap flagged above. `RndAvailabilitySchedule` had zero API surface; added full RND self-service CRUD (`GET/POST /rnd/availability/`, `PATCH/DELETE /rnd/availability/<id>/`) plus a public read (`GET /client/rnds/<id>/availability/`) for `rnd-profile-view.vue`'s previously-deferred availability grid. Reused the existing unused `RndAvailabilityScheduleSerializer`. **Deliberately dropped the mockup's "Block a Day Off" (date-range blocking) feature** — the real model only supports recurring weekly slots (`day_of_week` + `start_time`/`end_time`), there's no per-date blocking concept in it or in `vault/database.txt`, and adding an unscoped new model for it was explicitly ruled out in favor of shipping the real weekly-slot CRUD now. Also dropped the mockup's per-slot time-picker edit (was already a `console.log` stub) — a slot is added with a default 9AM–5PM window and can be removed/re-added rather than fake-editing in place. `RndProfileView.vue` (client-facing) now shows real per-day hours instead of nothing. Verified live end-to-end: RND added a Monday 9–5 slot, it persisted across reload, delete worked, and the same slot correctly appeared on the client-facing `rnd-profile-view.vue` grid. Test slot cleaned up after.
- [x] **`DashboardHome.vue` (RND dashboard) rebuilt for real data (2026-09-07) — scoped down from 8 tabs to Overview only** — 6 of the original 8 tabs (Patient Panel, Appointments, NCP Documentation, Meal Planning, Resources Library, Earnings & Billing) purely duplicated pages that are now real (MyPatients, Appointments, NCP Records, Meal Plans, Resources, Earnings); Settings duplicated `ProfileSettings.vue`'s real "Professional Profile" tab exactly. Dropped all 7, kept Overview as the only tab — sidebar nav already reaches the rest. Real data throughout: Active Patients/Today's Sessions/Earnings stat cards, Draft NCP Records (new `GET /rnd/ncp/drafts/` — no cross-patient draft list existed before, only per-relationship), Today's Schedule, New Patient Requests (with a real Accept action), Earnings Summary. Dropped "Avg. Goal Achievement" and "Patient Health Outcomes" stat cards entirely — no goal-tracking or outcomes-aggregation model exists to back them, and this project has consistently avoided faking numbers. **Along the way, found and fixed the biggest remaining gap in the whole project**: `NCPRecords.vue` (the 4-phase clinical NCP form) had zero backend API calls anywhere — entirely local component state, hardcoded patient name fallback (`route.query.patient`, not the real `?relationship=<id>` convention every other page already used to link to it), "Finalize" just flipped a local flag with nothing persisted. Despite this, it was never flagged as a gap anywhere and the endpoints (`GET/POST /rnd/relationships/<id>/ncp/`, `PATCH /rnd/ncp/<id>/`, `PATCH /rnd/ncp/<id>/finalize/`) had existed since Phase 3. Rewired for real: loads existing draft (if any) via the relationship id, lazy-creates on first "Save as Draft", saves/finalizes against the real record, locks all fields once finalized (immutable per RA 10173), real per-phase completion checklist. **Also fixed a real data-integrity gap while wiring it**: `bmi` on `NcpRecordSerializer` was a plain writable field that nothing ever set — always `null`. Made it read-only and computed server-side in `validate()` via the same `calculate_bmi`/`classify_bmi_asia_pacific` used by the screening flow, so NCP BMI is never client-supplied. Dropped the mockup's fake "linked meal plan" banner (`NcpRecord` has no such field) for a real link to the meal plan builder instead. Verified live end-to-end: full 4-phase flow (Assessment → PES Diagnosis → Intervention → Monitoring) against the real test client, correct server-computed BMI (24.2, WHO Asia-Pacific "Overweight (At Risk)"), draft persisted across reload, finalize locked the record and correctly blocked further edits, `client-detail.vue`'s phase-progress checklist picked up the real finalized record correctly. Full test suite (13 tests) still passes. Test records cleaned up after.
- [x] **Final mock-database sweep (2026-09-07): zero components reference `mock/mockDatabase.js` anymore** (`grep -rl "mockDatabase" frontend/app --include="*.vue"` returns nothing). Phase 6 frontend wiring is done — every dashboard page reads from the real Django backend. `mock/mockDatabase.js` itself can be deleted once confirmed nothing else imports it, but left in place for now (harmless, unreferenced).

## Phase 7 — Testing & polish
- [x] **Test suite for critical paths (2026-09-07)** — kept the existing `django.test.TestCase` + `manage.py test` convention (billing/tests.py's own style) rather than introducing pytest-django, since the 13 pre-existing tests already use it and there was no reason to run two test frameworks side by side. 60 new tests added across `accounts` (register, login, `/me`, full password-reset flow incl. wrong/expired/reused-code rejection), `scheduling` (relationship lifecycle, booking requires-active-relationship, the new screening-confirm gate, complete→invoice generation is idempotent, reviews), `clinical` (calculation engine — BMI/BMR/TDEE/NRS-2002 against the same hand-checked values documented in Phase 3, screening view, full NCP create/update/finalize lifecycle incl. server-side BMI recompute and cross-relationship scoping), and `communication` (messaging incl. third-party access denial and soft-delete, notifications). 73/73 tests pass (`manage.py test`, ~70s). Caught one real bug while writing fixtures: a test passed `date_of_birth` as a plain string to `ClientProfile.objects.create()` (bypasses serializer validation, so Django stored the raw string) — fixed to use a real `datetime.date`, not an app bug.
- [x] **Seed data management command (2026-09-07)** — `python manage.py seed`, idempotent (`update_or_create`, safe to re-run). Seeds the 6 real FNRI Food Exchange List categories (Rice/Meat/Vegetable/Fruit/Milk/Fat, matching `MealPlanMeal`'s existing `rice_exchanges`/`meat_exchanges`/etc. fields exactly) with standard per-exchange kcal/macros, plus 27 real common Filipino food items across those categories (local names, household measures, dietary flags — e.g. bangus/tilapia correctly flagged `ok_for_renal=False`, kangkong/ampalaya/sitaw flagged `is_free_food`). Also seeds 3 default `SystemSetting` rows (commission %, platform name, support email) — inert for now since nothing reads `SystemSetting` yet (`core`'s endpoints don't exist, per CLAUDE.md), but ready. **Deliberately does not create an admin user** — one already exists in dev, and `createsuperuser` is the correct/secure path (real password prompt, nothing hardcoded in a committed file) rather than duplicating that flow. Verified live: ran twice to confirm idempotency (second run: 0 created, same totals), confirmed real data via `/api/food-exchange/categories/` and `/api/food-exchange/items/?search=banana` (correctly matches both the Rice-category "Saba banana" and Fruit-category "Saging na lakatan"), and the diabetes-safe filter. **Found a real gap while verifying**: the RND sidebar's "Food Exchange Search" nav item links to `/food-exchange-search`, which doesn't exist as a page — 404s. `MealPlanning.vue` only has plain exchange-count number inputs, no searchable FNRI browser UI at all. Not fixed here (seed-data task, not a page-building one) — logged as its own gap below.
- [x] **`food-exchange-search.vue` built (2026-09-07)** — closes the dead `/food-exchange-search` nav link. No mockup existed for this page (checked `vault/frontend-design/` — never designed, not just never built), so the layout follows this project's own established pattern (`ResourceLibrary.vue`'s search-box + filter-chip + card-grid style) rather than porting anything. Category filter chips (colored per category, matching `FoodExchangeCategory.color`), 3 dietary-safety toggle filters (diabetes/hypertension/renal — all AND-composable with category and search), real per-item cards showing local name, household measure, exchange grams, and dietary flags (high sodium/potassium/high fiber/low GI/free food). All filtering happens client-side against the already-small seeded dataset (27 items) — no new backend needed, both endpoints (`GET /food-exchange/categories/`, `GET /food-exchange/items/`) already existed and were already correctly filterable server-side too. **Caught a real bug during verification**: the page rendered with no sidebar at all on first load — `definePageMeta` was only in the component (`FoodExchangeSearch.vue`), not the page file (`pages/food-exchange-search.vue`) — matches a pattern already seen elsewhere in this codebase (component-level `definePageMeta` is inert; only the page file's copy actually applies the `dashboard` layout). Fixed by adding it to the page file too. Verified live: real seeded data renders correctly (bangus/tilapia/chicken/egg/mongo correctly excluded by renal-safe, tofu correctly the only match; kangkong/ampalaya/sitaw correctly flagged Free Food + High Fiber), search correctly matches across categories (e.g. "banana" finds both the Rice-category "Saba banana" and the Fruit-category "Saging na lakatan"), category + dietary-safety filters compose correctly (AND logic).
- [x] **CORS/env sanity check (2026-09-07)** — already correctly env-driven (`CORS_ALLOWED_ORIGINS` via `python-decouple`, defaults to `localhost:3000`/`127.0.0.1:3000` for dev). Nothing to fix structurally; just needs the real deployed frontend URL set in prod `.env` at Phase 8, same as the other blank-until-deploy values.
- [x] **Rate limiting on auth endpoints (2026-09-07)** — DRF `ScopedRateThrottle`, applied only to the endpoints that are realistic abuse targets (no global `DEFAULT_THROTTLE_CLASSES`, so nothing else is throttled): login (10/min), register client+rnd (5/min each), password-reset request (5/min), password-reset confirm (10/min) — the last two matter most, since an unthrottled OTP endpoint is a real brute-force/spam vector. All 4 rates are env-overridable (`THROTTLE_*_RATE` in `.env`/`.env.example`) with the above as defaults. Verified live: 12 rapid login attempts against a real running server correctly allowed exactly 10 and 429'd the 11th/12th. Uses Django's default `LocMemCache` (no `CACHES` override) — fine for the current single-process dev server; a real production deploy with multiple workers will need a shared cache (Redis) for throttling to work correctly across processes, but that's a Phase 8 concern, not addressed here. Full test suite (73/73) still passes with throttling enabled.

## Real-time messaging (2026-09-07) — user-confirmed requirement, built same day
Replaces `Messages.vue`'s Phase 6 polling placeholder. Needs its own Redis instance — **Celery/background jobs were deliberately NOT set up alongside this**, even though it shares the same Redis; explicitly scoped out as separate, optional (Phase 5 is still "if needed"), future work, not bundled in just because the infra happened to be available.

- [x] Redis running locally via Docker (`docker run -d --name nutrimatch-redis -p 6379:6379 --restart unless-stopped redis:7-alpine`) — `REDIS_URL` env-configurable (`.env.example`), defaults to `redis://localhost:6379`.
- [x] `channels` + `channels-redis` + `daphne` installed; `daphne`/`channels` added to `INSTALLED_APPS` (daphne first, per Channels' own setup docs — it overrides `runserver` to serve ASGI). `ASGI_APPLICATION` + `CHANNEL_LAYERS` (`RedisChannelLayer`) added to settings.
- [x] `nutrimatch_api/channels_auth.py` — `JwtAuthMiddlewareStack`, since this app is JWT-only (no Django sessions) and Channels' built-in `AuthMiddlewareStack` only understands sessions. Browsers can't set custom headers on a WebSocket handshake, so the access token travels as a query param (`?token=<jwt>`) instead of a header — same constraint every WS-auth implementation hits.
- [x] `communication/consumers.py` (`MessageConsumer`) + `communication/routing.py` — one Channels group per relationship (`relationship_<id>`), same access rule as the REST endpoint (`RndClientRelationship` membership check at connect time, not per-message). Reuses the existing `MessageSerializer` for the broadcast payload (identical shape to the REST response) and the existing `notify()` service, so the message → notification pipeline didn't need touching.
- [x] `nutrimatch_api/asgi.py` rewired: `ProtocolTypeRouter` serving HTTP (unchanged DRF) and WebSocket (JWT-authed, routed to the consumer) from the same app.
- [x] **`Messages.vue` rebuilt on a real `WebSocket` connection** — no more `setInterval` polling. Connects per active conversation, sends/receives over the socket, auto-reconnects on drop (3s backoff, permanent-vs-transient close codes distinguished: 4001/4003 from the consumer's own auth/membership rejection don't retry). Chat header shows a real "Live"/"Connecting…" status, not a fake presence dot.
- [x] **Real bug found and fixed during testing**: a WebSocket that's been idle long enough can hit `channels_redis`'s pubsub read timeout and silently disconnect — any message sent to that relationship during the gap is safely persisted (confirmed via direct DB check) but the live push to that specific connection is lost, since a Channels group broadcast only reaches whoever was subscribed at that instant. Fixed with a reconnect-triggered silent re-sync (`loadMessageHistory(id, {silent:true})` on `onopen` after a reconnect) plus a `visibilitychange` listener as defense-in-depth for backgrounded-tab sockets that die without firing `onclose` promptly (mobile browsers, laptop sleep). Re-tested after the fix: live push confirmed working with no reload needed.
- [x] Backend test coverage: `communication/tests.py::MessageConsumerTests` using `channels.testing.WebsocketCommunicator` against the real Redis channel layer (not mocked) — connect-requires-membership, connect-succeeds-for-members, real bidirectional delivery between two separate connections (not just an echo), blank messages correctly ignored. `TransactionTestCase`, not `TestCase` — the consumer's DB access happens in a different async context than `TestCase`'s wrapping transaction allows. 77/77 tests pass overall (73 previous + 4 new).
- [x] Verified live end-to-end in the browser with two real logged-in sessions (client + RND): message sent by one appears instantly for the other with zero polling delay and zero page interaction, correctly persisted, correctly triggers the existing notification.

## Admin panel rebuild (2026-09-07) — a whole role missed by the Phase 6 sweep
The Phase 6 "zero mockDatabase.js references" sweep only checked one mock file — the entire admin side (7 pages + sidebar + notification/profile dropdowns) was still 100% driven by a **separate** mock file, `mock/mockAdminDatabase.js`, never caught until asked directly about the admin flow. All 7 pages rebuilt against real data in this pass.

**Backend additions:**
- `GET /api/admin/platform-stats/` — real aggregated numbers (active RNDs, clients, pending verifications, this-month commissions/revenue, 30-day new registrations, all-time completed consultations). No fake "platform uptime" — would need real infra monitoring this project doesn't have, dropped rather than fabricated.
- `GET /api/admin/rnds/` — pending + verified + deactivated RNDs in one list, with real `patients`/`consultations`/`average_rating`/`revenue` per RND via `Prefetch`, same N+1-avoidance pattern as `scheduling.RndPatientListSerializer`.
- `GET /api/admin/clients/` — real `condition`/`consultations`/`matched_rnd`/`last_active` per client, same prefetch pattern.
- `GET /api/admin/audit-logs/` (with `search`/`action` filters) — read-only, list-only (no PATCH/DELETE route at all, enforcing the model's own append-only design rather than just trusting admin discipline).
- `GET /api/admin/settings/` + `PATCH /api/admin/settings/<key>/` — generic key-value editor for `SystemSetting`, `key` itself is read-only (can't rename), tracks `updated_by`.
- `GET /api/admin/invoices/` (`AdminInvoiceListSerializer` in `billing/`) — platform-wide invoice list with both client and RND names, powers Billing & Commission's transaction log and per-RND payout summary (derived client-side, no separate payout endpoint).
- **`platform_commission_pct` now has a real effect**: `RndAppointmentCompleteView` reads it from `SystemSetting` when creating a new invoice (falls back to the `Invoice` model's own 10% default if unset/unparseable) — previously the admin-editable commission-rate setting existed but silently did nothing, since invoice creation never read it.
- RND "Reject" and "Suspend" both map onto the existing `is_active` toggle — there's no distinct "rejected" state in the schema, so this reuses the real mechanism (same one Client Management's deactivate uses) rather than inventing a new status. Client "Flagged" (payment disputes) dropped entirely — no backing model exists for it anywhere.
- 21 new backend tests across `accounts`, `core`, and `scheduling` (commission-rate wiring). 92/92 total pass.

**Two real bugs found and fixed while wiring, unrelated to any single page:**
1. `AdminDashboard.vue` never called its own `loadData()` — no `onMounted` hook existed, so stats stayed at their zero-initialized default and the pending-verifications panel showed "Loading…" forever. Real bug in code I'd just written, caught by actually testing in the browser instead of trusting the implementation.
2. `v-click-outside` was referenced in `NotificationDropdown.vue`/`ProfileDropdown.vue` but never registered as a directive anywhere in the app — Vue silently warned and no-opped it client-side (so it looked harmless), but an unregistered directive crashes Nuxt's SSR renderer (`ssrGetDirectiveProps` assumes the directive object exists). This was masked the whole time because these two components' original mock data was hardcoded to always be empty, so SSR never actually reached the `TransitionGroup` branch that uses the directive — real (non-empty) data exposed it immediately. Fixed with a real directive plugin (`frontend/app/plugins/click-outside.ts`, universal registration + client-only listener body) rather than removing the feature.

**Pages rebuilt, mockup-fakery dropped:**
- `AdminDashboard.vue` — real hero stats, real pending-RND-verification list with a link to review; dropped the fabricated "Recent System Activity" timeline (no real event-stream model backs a mixed activity feed) and the hardcoded "48 hours" SLA alert banner.
- `RndVerification.vue` — full real lifecycle (pending→approve→verified, verified→suspend→suspended→reinstate), dropped "View Credentials"/"View Profile" buttons (no credential file storage exists, same reasoning that blocked PDF/video resource uploads earlier in this project).
- `ClientManagement.vue` — real active/inactive counts and per-client detail, dropped the "Flagged" status UI and dispute-count stat entirely.
- `AuditLogs.vue` — real log stream with search/action-type filtering, dropped "Export Log", fake pagination ("Page 1 of 18"), and the fabricated "Events today/this week/Security alerts" snapshot numbers.
- `BillingCommission.vue` — real transaction log, real per-RND payout summary, commission-rate editor with genuine effect (see above); dropped the fake "payment gateway" text field (nothing reads/writes it), "Export CSV", and the fake payout-release workflow ("Released"/"Pending" status, "Release all pending payouts" button) — no payout-tracking model exists, invoices track client→platform payment only, platform→RND payout is a real, separate, unbuilt feature.
- `SystemSettings.vue` — real generic settings editor (any `SystemSetting` row, not just commission), a compliance panel restated as accurate present-tense claims rather than the mockup's aspirational ones; dropped admin account-edit fields (no writable name/email endpoint exists anywhere in the app, not just here), all 4 config toggles (RND verification mode / client registration / audit notifications / maintenance mode — none backed by any model), and the entire "danger zone" (a "purge audit logs" button directly contradicts the immutable-by-design principle stated one panel over; "enable maintenance mode" has no real effect anywhere in the stack).
- `PlatformReports.vue` — real stat cards + real RND performance table (added a `consultations` field to `AdminRndListSerializer` for this); dropped the hardcoded client-condition-distribution chart (would need a new `medical_conditions` aggregation, scoped out) and the entire "Generate Custom Report" panel (fake PDF/CSV/XLSX export via `setTimeout`, no real export capability exists).
- `AdminSidebar.vue` / `ProfileDropdown.vue` / `NotificationDropdown.vue` — real logged-in admin name/email/initials throughout (was hardcoded "Elvi Lito Ubas" / "Ivy Hope O. Alba, RND" — the latter nonsensically showing RND credentials inside the admin panel), real sign-out (previously just cleared a `localStorage` key without touching the auth store), real pending-RND-count sidebar badge, and a real (if currently empty) notifications feed via the existing `/notifications/` endpoint instead of fabricated names/messages.
- Verified every page live in the browser against real seeded + throwaway test data, cleaning up test data after each check.

## Phase 8 — Deployment
- [ ] Django: Railway/Render/Fly.io (or your school's requirement) + Postgres addon
- [ ] Nuxt: Vercel/Netlify, or same host as backend
- [ ] Environment variables for prod (PayMongo live keys, Daily.co)
- [ ] Retire/archive `backend/` (Laravel) once Django reaches parity — don't delete until demo is confirmed working

## Open questions to resolve early (ask your adviser / check the PDF)
- [ ] Does the capstone require a specific deployment target or grading rubric that assumes Laravel?
- [x] **Resolved (2026-09-07): real-time messaging IS required**, not polling-acceptable-for-MVP — and built the same day (see the dedicated entry below). `Messages.vue`'s 5s-poll implementation from the Phase 6 entry above is retired.
- [ ] Check `feature/kent-dashboard` branch (skipped for now) for any work not yet in `master` before it's forgotten
